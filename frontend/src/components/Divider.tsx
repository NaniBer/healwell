/**
 * Divider Component
 * A simple horizontal divider line for separating sections.
 */

import React from 'react';

const Divider: React.FC = () => {
  return (
    <div className="w-full h-px bg-healwell-lightGray" />
  );
};

export default Divider;
